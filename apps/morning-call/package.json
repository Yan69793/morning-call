{
  "name": "@sz/morning-call",
  "version": "0.0.1",
  "private": true,
  "description": "Morning Call multimercados: brief macro rastreável (Cloudflare Worker)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json",
    "gen:schema": "tsx scripts/gen-trade-card-schema.ts",
    "archive": "tsx scripts/save-morning-call.ts",
    "smoke:codex": "tsx scripts/local/smoke-transporte.ts"
  },
  "dependencies": {
    "@sz/analytics": "*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260715.1",
    "@types/node": "^24.13.3",
    "tsx": "^4.23.1",
    "wrangler": "^4.111.0"
  }
}
