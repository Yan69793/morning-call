{
  "name": "sz-briefing-remote",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Execucao remota do Briefing Matinal (Cloudflare Worker + Durable Object + KV). Porta do pipeline briefing-interno para rodar com o PC desligado. Nao faz parte dos npm workspaces do monorepo.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
